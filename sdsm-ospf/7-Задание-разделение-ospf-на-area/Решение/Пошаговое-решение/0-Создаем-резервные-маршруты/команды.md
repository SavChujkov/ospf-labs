

# backbone-1 резервные маршруты

#до eigrp AS
ip route 10.0.20.0 255.255.255.0 10.0.10.29



# office-backup

#маршрут до сетей в area 0
ip route 10.0.10.0 255.255.255.0 10.0.10.30

#маршрут до 0.0.0.0, так как default originate от backbone-1 удалится
ip route 0.0.0.0 0.0.0.0 10.0.10.28


#временный анонс в eigrp статических маршрутов
#чтобы внутри eigrp при удалении маршрутов ospf осталась связность
router eigrp 1
redistribute static


# проверка маршрута до EIGRP сетей с backbone-1
    - маршрут теперь через статику до EIGRP

        backbone-1#show ip route 10.0.20.0
        Routing entry for 10.0.20.0/24
        Known via "static", distance 1, metric 0
        Routing Descriptor Blocks:
        * 10.0.10.28
            Route metric is 0, traffic share count is 1

# проверка маршрута до area 0 с office-backup
    - маршрут до любой неизвестной /30 подсети из диапазона 10.0.10.0/24 
      лежит через backbone-1

        office-backup#show ip route 10.0.10.0
        Routing entry for 10.0.10.0/24
        Known via "static", distance 1, metric 0
        Routing Descriptor Blocks:
        * 10.0.10.30
            Route metric is 0, traffic share count is 1

# проверяем доступность EIGRP  с DC-LOCAL-Router

router eigrp 1
 redistribute ospf 2 metric 1000 100 255 1 1500 route-map OSPF-TO-EIGRP
 network 10.0.20.8 0.0.0.3
 no auto-summary
